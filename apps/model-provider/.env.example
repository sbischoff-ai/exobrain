APP_ENV=local
LOG_LEVEL=DEBUG
MODEL_PROVIDER_CONFIG_PATH=config/models.yaml
MODEL_PROVIDER_TIMEOUT_SECONDS=60

# Upstream provider credentials are required in your shell environment when aliases use them.
# Example:
#   export OPENAI_API_KEY=...
#   export ANTHROPIC_API_KEY=...
